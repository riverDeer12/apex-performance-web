/**
 * Represents the days of the week.
 */
export enum DayOfWeek {
    /** Indicates Sunday. */
    Sunday,
    /** Indicates Monday. */
    Monday,
    /** Indicates Tuesday. */
    Tuesday,
    /** Indicates Wednesday. */
    Wednesday,
    /** Indicates Thursday. */
    Thursday,
    /** Indicates Friday. */
    Friday,
    /** Indicates Saturday. */
    Saturday,
}
